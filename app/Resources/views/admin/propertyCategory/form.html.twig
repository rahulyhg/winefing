<div class="modal fade" tabindex="-1" role="dialog" id="propertyCategory">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            {{ form_start(form,  { 'attr': {'required': 'true', 'id' : 'propertyCategory'} }) }}
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Ajouter/modifier une catégorie de propriété</h4>
                </div>
                <div class="modal-body">
                    <label>Name</label>
                    {% for propertyCategoryTr in form.propertyCategoryTrs %}
                        <fieldset class="form-group">
                            <div class="row">
                                <div class="col-md-2">
                                    {{ form_widget(propertyCategoryTr.language,  { 'attr': {'disabled': 'false'} }) }}
                                </div>
                                <div class="col-md-10">
                                    {{ form_widget(propertyCategoryTr.name,  { 'attr': {'class': 'form-control', 'style': 'width: 50%;'} }) }}
                                    {{ form_widget(propertyCategoryTr.id, {'attr': {'style': 'display: none'}}) }}
                                </div>
                            </div>
                        </fieldset>
                    {% endfor %}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                    <button type="submit" class="btn btn-primary pull-right">Valider</button>
                </div>
            {{ form_end(form) }}
        </div><!-- /.modal-content -->
    </div><!-- /.modal -->
</div>