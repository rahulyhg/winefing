{% extends 'admin/base.html.twig' %}
{% block table %}
    <table class="table table-striped table-responsive">
        <thead>
        <tr>
            <th>
                <button type="button" class="btn btn-default" onclick="loadForm('{{ url('subscription_new_form')}}', 'subscription')">
                    <span class="glyphicon glyphicon-plus-sign"></span>
                </button>
                Abonnement
            </th>
            <th>Description</th>
            <th>Format</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for subscription in subscriptions %}
            <tr>
                <td data-toggle="collapse" data-target="#characteristics-{{ subscription.id }}" class="accordion-toggle" style="cursor:pointer">
                    {% for subscriptionTr in subscription.subscriptionTrs %}
                        <object style="float: left;" type="image/svg+xml" data="{{ languagePicturePath }}{{ subscriptionTr.language.picture }}" height="20px">
                        </object>
                        <span style="margin-left: 10px">{{ subscriptionTr.name }}</span>
                        <br><br>
                    {% endfor %}
                </td>
                <td>
                    {% for subscriptionTr in subscription.subscriptionTrs %}
                        {{ subscriptionTr.description }}
                        <br><br>
                    {% endfor %}
                </td>
                <td>
                    {{ subscription.format }}
                </td>
                <td>
                    <input type="button"  class="btn btn-primary" value="Editer" id="update" onclick="loadForm('{{ path('subscription_new_form', {'id' : subscription.id}) }}', 'subscription')"/>
                    <input type="button" class="btn btn-primary" value="Suprimer" onclick="setPathDeleteButton('{{ path('subscription_delete', {'id' : subscription.id}) }}')"/>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
{% block modal %}
    <span id="modalDelete">
        <div class="modal fade" tabindex="-1" role="dialog" id="confirmation">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        <h4 class="modal-title">Suppression</h4>
                    </div>
                    <div class="modal-body">
                        <p>Etes-vous sur de vouloir supprimer cet élément ?</p>
                    </div><!-- /.modal-content -->
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
                        <a id="delete" type="submit" class="btn btn-primary pull-right">Valider</a>
                    </div>
                </div><!-- /.modal-dialog -->
            </div><!-- /.modal -->
        </div>
    </span>
    <span id="modalCreate">
    </span>
{% endblock %}
{% block script %}
    <script>
        function setPathDeleteButton(path) {
            $("#delete").attr("href", path);
            $('#confirmation').modal();
        }
        function loadForm(path, modalId) {
            $.ajax({
                url:  path,
                type: 'GET',
                async: false,
                success: function (data, textStatus, jqXHR) {
                    $('#modalCreate').html(data);
                    $('#'+modalId).modal();
                },
                error: function (jqXHR, textStatus, errorThrown) {
                    console.log("no good");
                }
            });
        }
    </script>
    <script>
        $('#subscription').live('submit', function (event) {
            $("#subscription :disabled").removeAttr('disabled');
        });
    </script>
{% endblock %}
